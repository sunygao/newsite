import Config from 'config/config';
import CV from 'config/CV';
import Section from 'abstract/section';

//home page video header
export default class ImageArticle extends Section {
  constructor(options) {
    super(options);
  }

}
